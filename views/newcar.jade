extends layout
block content

	h1 Godkänna ny användare
	if authorizedUser.length>0
		form(action="/authorized", method="POST")
			table(border='1')
				thead
					tr
						th Namn
						th Anställningsnummer
						th Godkänd

				each user in authorizedUser
					tr
						td= user.name
						td= user['id-number']
						td 
							input(type="checkbox" name="userOk" id="ok" value=user['id-number'])
			input(type="submit" class="btn btn-primary btn-block" value="Godkänna")
	else
			p Det finns inga nya användare att godkänna

	h1 Lägg till ny bil
	form(action="/newcar", method="POST")
		div
		lable(for="reg") Reg.nr:
			input(type="text", id="reg" name="registration")  
		br     
		div
			lable(for="gearbox") Växellåda:
				select(type="text", id="gearbox", name="gearbox") 
					option automat
					option manuell
		br
		div
			lable(for="fuel") Drivmedel:
				select(type="text", id="fuel", name="fuel")
					option bensin
					option diesel
		br 
		div
			lable(for="service") Besiktigad:
				select(type="text", id="service", name="service") 
					option januari
					option februari
					option mars
					option april
					option maj
					option juni
					option juli
					option augusti
					option september
					option oktober
					option november
					option december
		br
		div
			lable(for="type") Fordonstyp: 
				select(type="text",id="type", name="type") 
					option liten
					option mellan
					option stor
					option servicebil
					option skåpbil
					option tjänstebil
		br
		div
			lable(for="model") Modell: 
				input(type="text",id="model", name="model")
		br 
		button(type="submit") Lägg till
	if posted
		if checkedCar
			h4 Det finns redan en bil med det registreringsnumret
		else
			h4 Följande bil är tillagd i registret: #{newCar.registration}, #{newCar.model}
